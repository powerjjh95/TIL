K, N = map(int, input().split())
length_list = []
for _ in range(K):
    K_length = int(input())
    length_list += [K_length]
    N_length =
    for i in range(len(length_list)):
        while True:
            if length_list[i]